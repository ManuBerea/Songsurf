<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialComponentsTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.playlist.VideoPlayerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.playlist.BasePlaylistActivity"
            android:exported="false" />
        <activity
            android:name=".ui.playlist.PlaylistActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MyAccountActivity"
            android:exported="false" />
        <activity
            android:name=".ui.playlist.MyPlaylistsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.playlist.AddToPlaylistActivity"
            android:exported="false" />
        <activity
            android:name=".ui.FilterActivity"
            android:theme="@style/MaterialComponentsTheme">
            >
        </activity>
        <activity
            android:name=".ui.authentication.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.authentication.SignUpActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar" />
    </application>

</manifest>